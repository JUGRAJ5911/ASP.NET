<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Propensity Tracker</title>
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" type="text/css" />
    <script src="~/Scripts/modernizr-2.8.3.js"></script>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        header {
            background-color: #f8f9fa;
            border-bottom: 1px solid #e9ecef;
            text-align: center;
            padding: 10px 0;
            width: 100%;
        }

        footer {
            background-color: #f8f9fa;
            border-top: 1px solid #e9ecef;
            text-align: center;
            padding: 10px 0;
            width: 100%;
            margin-top: auto;
        }

        main {
            flex: 1;
            padding: 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            box-sizing: border-box;
        }

        .form-container {
            border: 1px solid #ddd;
            padding: 20px;
            border-radius: 8px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            min-width: 300px;
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
            align-items: center;
            width: 80%;
            max-width: 900px;
            margin-top: 20px;
            margin-bottom: 20px;
        }

        .form-title {
            margin-bottom: 20px;
        }

        .btn-custom {
            background-color: #007bff;
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
            text-decoration: none;
        }

            .btn-custom:hover {
                background-color: #0056b3;
            }

        .button-group {
            display: flex;
            gap: 10px;
        }

        .text-success {
            color: green;
        }

        .text-danger {
            color: red;
        }

        .table-responsive {
            width: 100%;
            overflow-x: auto;
            display: flex;
            justify-content: center;
        }

        .styled-table {
            border-collapse: collapse;
            margin: 25px 0;
            font-size: 0.9em;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-width: 400px;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.15);
            align-self: center;
            width: 100%;
        }

            .styled-table thead tr {
                background-color: #009879;
                color: #ffffff;
                text-align: left;
            }

            .styled-table th,
            .styled-table td {
                padding: 12px 15px;
                border: 1px solid #dddddd;
                text-align: center;
            }

            .styled-table tbody tr {
                border-bottom: 1px solid #dddddd;
            }

                .styled-table tbody tr:nth-of-type(even) {
                    background-color: #f3f3f3;
                }

                .styled-table tbody tr:last-of-type {
                    border-bottom: 2px solid #009879;
                }

        .form-box {
            border: 1px solid #ddd;
            padding: 20px;
            border-radius: 8px;
            background-color: #fff;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-top: 20px;
            margin-bottom: 20px;
        }

        .back-link {
            margin-top: 10px;
        }

        .delete-box {
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f8f9fa;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 20px;
            margin-bottom: 20px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

            .delete-box h3 {
                margin-top: 0;
                color: #dc3545; /* Red color for emphasis */
            }

            .delete-box h4 {
                margin-top: 0;
                font-size: 1.5em;
                color: #343a40; /* Dark color for headings */
            }

            .delete-box .dl-horizontal {
                font-size: 1.1em;
                margin-bottom: 20px;
            }

                .delete-box .dl-horizontal dt {
                    font-weight: bold;
                    color: #007bff; /* Blue color for labels */
                }

                .delete-box .dl-horizontal dd {
                    margin-left: 0;
                }

            .delete-box .form-actions {
                margin-top: 20px;
            }

        /* Custom styling for HR */
        hr.style-five {
            border: 0;
            height: 1px;
            background: #999;
            background-image: linear-gradient(to right, #f0f0f0, #999, #f0f0f0);
        }

        /* Button hover effect */
        .btn-danger:hover {
            background-color: #dc3545;
            border-color: #dc3545;
        }

        /* Styles for edit box */
        .edit-box {
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f8f9fa;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 20px;
            margin-bottom: 20px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

            .edit-box h2 {
                margin-top: 0;
                color: #007bff; /* Blue color for header */
            }

            .edit-box h4 {
                margin-top: 0;
                font-size: 1.5em;
                color: #343a40; /* Dark color for headings */
            }

            .edit-box .form-horizontal {
                font-size: 1.1em;
            }

            .edit-box .form-group {
                margin-bottom: 20px;
            }

            .edit-box .control-label {
                font-weight: bold;
                color: #007bff; /* Blue color for labels */
            }

            .edit-box .form-control {
                border-color: #007bff; /* Blue color for input borders */
            }

            .edit-box .btn-primary {
                background-color: #007bff;
                border-color: #007bff;
            }

                .edit-box .btn-primary:hover {
                    background-color: #0056b3; /* Darker blue on hover */
                    border-color: #0056b3;
                }

            .edit-box .back-link {
                margin-top: 10px;
            }

        /* Custom styling for HR */
        hr.style-five {
            border: 0;
            height: 1px;
            background: #999;
            background-image: linear-gradient(to right, #f0f0f0, #999, #f0f0f0);
        }

        /* Styles for create box */
        .create-box {
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f8f9fa;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 20px;
            margin-top: 20px;
            margin-bottom: 20px;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

            .create-box h2 {
                margin-top: 0;
                color: #28a745; /* Green color for header */
            }

            .create-box h4 {
                margin-top: 0;
                font-size: 1.5em;
                color: #343a40; /* Dark color for headings */
            }

            .create-box .form-horizontal {
                font-size: 1.1em;
            }

            .create-box .form-group {
                margin-bottom: 20px;
            }

            .create-box .control-label {
                font-weight: bold;
                color: #28a745; /* Green color for labels */
            }

            .create-box .form-control {
                border-color: #28a745; /* Green color for input borders */
            }

            .create-box .btn-success {
                background-color: #28a745;
                border-color: #28a745;
            }

                .create-box .btn-success:hover {
                    background-color: #218838; /* Darker green on hover */
                    border-color: #218838;
                }

            .create-box .back-link {
                margin-top: 10px;
            }

        /* Custom styling for HR */
        hr.style-five {
            border: 0;
            height: 1px;
            background: #999;
            background-image: linear-gradient(to right, #f0f0f0, #999, #f0f0f0);
        }

        .logout-link {
            position: absolute;
            top: 10px; /* Adjust top position as needed */
            right: 20px; /* Adjust right position as needed */
            text-decoration: none;
            color: #007bff; /* Blue color for link */
            font-weight: bold;
        }

            .logout-link a:hover {
                color: #0056b3; /* Darker blue on hover */
            }
    </style>
</head>
<body>
    <header>
        <h1>Propensity Tracker</h1>
    </header>

    <main>
        <div class="form-container">
            @RenderBody()
        </div>
    </main>

    <footer>
        <p>
            Jugrajsingh Rajvindersingh Dhillon - 200539521<br> Arin Rao - 200562511<br> Raunak Raut - 200560254<br> COMP2084 - Server-Side Scripting-ASP.NET<br> Hossein Hesami
        </p>
    </footer>

    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <script src="~/Scripts/bootstrap.min.js"></script>
</body>
</html>
